<!DOCTYPE html>
<html>
    <head>
        <title>{{ __('Error') }}</title>
    </head>
    <body>
        <h1>{{ __('Error') }}</h1>
        <img src="{{ $url }}" alt="{{ __('Error Image') }}">
    </body>
</html>
